<div class="container p-4">
    <div class="row">
        <div class="col-md-5 mx-auto">
            <div class="card text-center p-4 bg-dark">
                <div class="car-body text-white">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/links" class="btn btn-secondary">Save your links</a>
                </div>
            </div>
        </div>
    </div>
</div>